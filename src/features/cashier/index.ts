/**
 * Cashier feature entry — the route renders `CashierScreen`, which is the
 * Bills list (matching the old contract). Detail is presented from within.
 */
export { BillsScreen as CashierScreen } from "./BillsScreen";
